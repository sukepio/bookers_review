<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<%= form_with model: @book, local: true do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <%= f.submit '送信' %>
<% end %>